# ═══ PaymentRouter Docker 环境配置 ═══
# 复制为 .env 后修改

APP_ENV=development
APP_SECRET=change-me-to-a-random-string
APP_PORT=8080

# MySQL
DB_HOST=mysql
DB_PORT=3306
DB_NAME=payment_router
DB_USER=payment_router
DB_PASSWORD=payment_router_secret
DB_ROOT_PASSWORD=root_secret_change_me
