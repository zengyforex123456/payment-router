# ═══ Converge Crontab ═══
# 部署后执行: crontab docker/crontab.txt
# 或: cp docker/crontab.txt /etc/cron.d/converge

# ── 每 5 分钟: 智能流量分配 (Thompson Sampling 权重更新) ──
*/5 * * * * php /var/www/converge/scripts/cron-smart-rotation.php >> /var/log/converge-rotation.log 2>&1

# ── 每 10 分钟: 运营自动化规则 (ROI/转化率/Conversion Cap) ──
*/10 * * * * php /var/www/converge/scripts/cron-auto-rules.php >> /var/log/converge-rules.log 2>&1

# ── 每 30 分钟: Facebook 花费同步 ──
*/30 * * * * php /var/www/converge/scripts/cron-facebook-cost.php >> /var/log/converge-cost.log 2>&1

# ── 每天凌晨 3 点: 数据清理 (保留 LOG_RETENTION_DAYS 天) ──
0 3 * * * php /var/www/converge/scripts/cron-cleanup.php >> /var/log/converge-cleanup.log 2>&1
